<p>
  <em><strong>Competency Profile (headings need to be translated into French):</strong></em>
</p>

<p>
  <strong><u>Organisational competencies:</u></strong>
</p>
<ol class="competencies">
  <% @organisational.each do |o| %>
    <li>
      <%= o.competency_french %>
    </li>
  <% end %>
</ol>

<p>
  <strong><u>Functional competencies:</u></strong>
</p>

<p>
  <strong>Technical competencies:</strong>
</p>
<ol class="competencies">
<% @position.functionals.each do |f| %>
  <li>
    <% @dictionary.each do |d| %>
      <% if d.id == f.indicator %>
        <%= d.competency_french %> (Im: <%= f.importance %> Val: <%= f.value %>)
      <% end %>
    <% end %>
  </li>
<% end %>
</ol>

<p>
  <strong>Method related competencies:</strong>
</p>
<ol class="competencies">
<% @position.methodrelations.each do |m| %>
  <li>
    <% @dictionary.each do |d| %>
      <% if d.id == m.indicator %>
        <%= d.competency_french %> (Im: <%= m.importance %> Val: <%= m.value %>)
      <% end %>
    <% end %>
  </li>
<% end %>
</ol>

<p>
  <strong>Leadership competencies:</strong>
</p>
<ol class="competencies">
<% @position.leaderships.each do |l| %>
  <li>
    <% @dictionary.each do |d| %>
      <% if d.id == l.indicator %>
        <%= d.competency_french %> (Im: <%= l.importance %> Val: <%= l.value %>)
      <% end %>
    <% end %>
  </li>
<% end %>
</ol>

<p>
  <strong>Social competencies:</strong>
</p>
<ol class="competencies">
<% @position.socials.each do |s| %>
  <li>
    <% @dictionary.each do |d| %>
      <% if d.id == s.indicator %>
        <%= d.competency_french %> (Im: <%= s.importance %> Val: <%= s.value %>)
      <% end %>
    <% end %>
  </li>
<% end %>
</ol>
