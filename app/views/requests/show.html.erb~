<%- model_class = Request -%>
<div class="page-header">
  <h1>Competency <%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong>Proposed competency:</strong></dt>
  <dd><%= @request.request %></dd>
  <dt><strong>Date request:</strong></dt>
  <dd><%= @request.created_at.strftime("%d %B %Y") unless @request.created_at.nil?  %></dd>
  <dt><strong>Requester:</strong></dt>
  <dd><%= @request.user %></dd>
  <dt><strong><%= model_class.human_attribute_name(:office) %>:</strong></dt>
  <dd><%= @request.office_name %></dd>
  <dt><strong><%= model_class.human_attribute_name(:status) %>:</strong></dt>
  <dd><%= @request.status_outcome %></dd>
</dl>

<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              dictionaries_office_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_request_path(@request), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              request_path(@request),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
